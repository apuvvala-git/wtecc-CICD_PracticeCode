name: CI workflow for Coursera IBM CICD Coursera

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
    
jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: python:3.9-slim
